name: ozzie
description: A new flutter package project.
version: 1.0.0
author: Jorge Coca<jcocaramos@gmail.com>
homepage: Ozzie is your testing friend. Ozzie will take an screenshot during integration tests whenever you need.

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_driver:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: 3.0.0

flutter:
